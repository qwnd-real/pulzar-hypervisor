[package]
name = "processor"
description = "What the processor pulzar runs on can do, asked once and cached"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
bitflags = { workspace = true }
log = { workspace = true }
raw-cpuid = { workspace = true }
spin = { workspace = true }

[lints]
workspace = true
