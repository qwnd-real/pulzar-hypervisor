[package]
name = "handoff"
description = "Boot protocol handed from hv-loader to the pulzar hypervisor image"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
thiserror = { workspace = true }
uefi-raw = { workspace = true }

[lints]
workspace = true
