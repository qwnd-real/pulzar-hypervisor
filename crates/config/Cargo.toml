[package]
name = "config"
description = "The machine's own choices, recorded where every part of the hypervisor can read them"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
# The type of the seed: a configuration value is only as good as the
# transform that consumes it, so the key and what it keys live together.
spoof = { workspace = true }

[lints]
workspace = true
