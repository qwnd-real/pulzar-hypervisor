# 2026-07-31

- Created `tmp/review/vlapic-review-prompts.md`: 15 standalone, read-only prompts covering every `crates/vlapic/src/*.rs` file. Each forbids subagents, scopes findings to vlapic, requires severity-sorted reports, and mandates exhaustive item ledgers, Intel/AMD xAPIC/x2APIC specification citations, full register/mode/event matrices, concurrency proofs, host-LAPIC passthrough safety analysis, cross-file contract checks, and later aggregation.
- Extended every vLAPIC prompt to use the supplied corpus in `tmp/review/vlapic/output/`: start from `output.md`, open only specifically needed LAPIC diagram images, and use KVM's `lapic.c`/`lapic.h` as a qualified implementation reference rather than a specification, explicitly accounting for KVM's full emulation versus Pulzar's selective physical-LAPIC passthrough/reprogramming design.
- Completed the read-only architectural review of `crates/vlapic/src/priority.rs` and wrote `tmp/review/vlapic-priority.md`. The review found one definitive medium-severity issue: `arbitration_priority` unconditionally implements Intel P6 APR arithmetic even though Pulzar exposes an AMD CPU contract; all other priority representation, PPR, legality, deliverability, concurrency, passthrough-safety, and bounded-scan behaviors were compliant or rejected as false positives. The local Git executable could not run because of a Cygwin `FAST_CWD` failure, so write-scope verification used the operation audit and report/source readback.
- Completed the read-only review of `crates/vlapic/src/error.rs` and wrote `tmp/review/vlapic-error.md`. The review found one high-severity defect (`ERROR-H1`): `ErrorStatus::record()` returns the one-shot APIC-error trigger, but callers discard or repurpose it and never inject the guest LVT Error vector. ESR pending/latched semantics, reset behavior, and atomic interleavings were otherwise found compliant. Native Git status verification remained unavailable because the environment's Cygwin layer exits with a `FAST_CWD` error.
- Completed and verified the read-only architectural audit report `tmp/review/vlapic-mmio.md` for `crates/vlapic/src/mmio.rs`. Accepted findings: `MMIO-H1` (MMIO remains live outside xAPIC mode), `MMIO-M1` (reserved accesses record ESR but do not deliver armed LVT Error), and `MMIO-M2` (valid accesses synchronously log at error level). Corrected the APIC aperture spelling to `0xFEE0_0000` and made the timer citation exact (`timer.rs:72-104`). Structural checks passed; native Git status remained unavailable because the environment's Cygwin layer exits with `FAST_CWD`, so sole-output confirmation relies on the file-operation audit.
- Completed the exhaustive read-only audit of `crates/vlapic/src/icr.rs` and wrote `tmp/review/vlapic-icr.md`. Accepted findings: `ICR-H1` (x2APIC reserved fields are stored/dispatched instead of `#GP(0)`), `ICR-H2` (invalid delivery/shorthand combinations can execute, including INIT broadcasts), `ICR-M1` (illegal Fixed/Lowest vectors are attributed to target `Receive Illegal Vector` rather than source `Send Illegal Vector`), and `ICR-L1` (xAPIC ICR-high reserved bits persist/read back). The report includes the complete item ledger, Intel/AMD/KVM basis, physical INIT/SIPI ownership analysis, rejected candidates, and test gaps. Git status remained unavailable due the environment's Cygwin `FAST_CWD` failure, so the sole-output statement is based on the tool operation audit and readback.
- Completed the read-only architectural audit of `crates/vlapic/src/timer.rs` and wrote `tmp/review/vlapic-timer.md`. The report includes Intel SDM/AMD APM basis, selected diagram evidence, KVM comparison, an exhaustive timer ledger, cross-file contracts, findings TIMER-C1 through TIMER-C7, lifecycle and concurrency analysis, rejected candidates, and test gaps. No source code was modified; the report is the only file written for this audit.
- Completed the exhaustive read-only review of `crates/vlapic/src/lib.rs` and wrote `tmp/review/vlapic-lib.md`. Accepted findings: five High (`LIB-H1` partial Once publication; `LIB-H2` illegal-vector physical EOI debt; `LIB-H3` non-bijective deferred physical EOI accounting/priority; `LIB-H4` mode-change debt and physical-state loss; `LIB-H5` contract-dependent xAPIC logical routing on physical x2APIC) and one Medium (`LIB-M1` software-disable omits physical timer reconciliation). The report includes exact Intel/AMD/KVM bases, a complete ledger, cross-file contracts, rejected candidates, and test gaps; no source code was modified.
- Completed the exhaustive read-only audit of `crates/vlapic/src/register.rs` and wrote `tmp/review/vlapic-register.md`. Accepted one definitive high-severity root defect, `REGISTER-H1`: `Access::of` leaves the register file active in globally disabled mode and lacks enough face information to prevent legacy MMIO from remaining live in x2APIC mode, allowing virtual and selected physical LAPIC side effects. Offset/MSR conversion, bank arithmetic, active-face register classes, lifecycle, concurrency, and passthrough contracts were otherwise compliant or rejected as false positives. Native Git status verification remained unavailable due the Cygwin `FAST_CWD` failure; sole-output confirmation relies on the operation audit and report readback.
- Completed and verified the production-grade read-only audit of `crates/vlapic/src/access.rs` in `tmp/review/vlapic-access.md`. Accepted 11 findings: one Critical (mode changes erase deferred physical EOI debt and fail to reconcile hardware), four High (software-disable masking, timer-LVT implicit restart, deadline-mode initial-count handling, and x2APIC RsvdZ gaps), five Medium (ICR error attribution, missing virtual Error-LVT injection, LVT vector-validation condition, stale LINT Remote IRR, and DCR timer reload), and one Low (xAPIC ICR-high reserved-bit readback). The report includes exact current line anchors, complete register/mode/event/concurrency ledgers, cross-file contracts, rejected candidates, and test gaps. Git status again failed before execution because of Cygwin `FAST_CWD`; write-scope confirmation relies on the operation history and full report/source readback.
- Completed the exhaustive concurrency and architecture review of `crates/vlapic/src/vectors.rs` and wrote `tmp/review/vlapic-vectors.md`. Accepted findings: `VECTORS-H1` (non-transactional bitmap/controller reset can race with physical arrival, corrupt IRR/TMR and lose a physical EOI debt), `VECTORS-H2` (one deferred bit keyed by guest vector cannot represent multiple remapped physical ISR debts), and `VECTORS-L1` (`take_highest` documentation contradicts its scan-then-clear implementation and single-clearer contract). The report proves all 256 vector boundaries, covers every item/ordering/caller role, records Intel/AMD/KVM bases, rejects false positives, and identifies the release/acquire versus `away` lost-wakeup proof as blocked/contract-dependent. Native Git verification remained unavailable due the Cygwin `FAST_CWD` failure; sole-output confirmation relies on the operation audit and report readback.
- Completed and final-verified the exhaustive read-only audit of `crates/vlapic/src/delivery.rs` in `tmp/review/vlapic-delivery.md`. Accepted findings: one High (`DELIVERY-H1`, compound INIT/SIPI atomics lose or mis-epoch concurrent events) and six Medium (`DELIVERY-M1` illegal-vector ESR attribution/LVT Error, `M2` disabled-target delivery, `M3` incorrect AMD Lowest Priority arbitration, `M4` >256 target truncation, `M5` stranded requests on doorbell failure, and contract-dependent `M6` dropped SMI). The report includes the complete compliance ledger, current root-line verification, cross-file contracts, concurrency interleavings, rejected candidates, diagrams used, and test gaps. Git commands again exited on the Cygwin `FAST_CWD` warning before producing a trustworthy status, so sole-output confirmation relies on the complete operation audit and report/source readback.
- Completed the exhaustive read-only audit of `crates/vlapic/src/msr.rs` and wrote `tmp/review/vlapic-msr.md`. Accepted three High findings (missing x2APIC ICR RsvdZ checks, missing register-specific low-dword RsvdZ checks, and incorrect TSC-deadline read/ignored-write physical side effects) and two Medium findings (CPUID/model feature gating and missing sender-side illegal-vector ESR reporting). The report includes Intel/AMD/KVM sources, four selectively opened diagrams, a complete compliance ledger, cross-file/passthrough/concurrency analysis, rejected candidates, and test gaps. Git status remained unavailable due the Cygwin `FAST_CWD` failure; sole-output confirmation relies on the operation audit and full report readback.
- Completed and verified the exhaustive read-only audit of `crates/vlapic/src/lvt.rs` in `tmp/review/vlapic-lvt.md`. Accepted findings: two High (`LVT-H1`, unconditional Intel seven-entry/CMCI topology under an exposed AMD CPU contract; `LVT-H2`, AMD Error-LVT Message Type writes discarded), three Medium (`LVT-M1`, illegal-vector validation uses field presence rather than effective Fixed delivery; `LVT-M2`, Timer bit 18/deadline mode lacks feature/vendor gating; `LVT-M3`, Delivery Status and Remote IRR never reflect controller state), and one Low (`LVT-L1`, reserved-delivery documentation contradicts retained readback). The report inventories both opened Figure 13-8 extractions, includes the complete item ledger and cross-file/host-safety analysis, and records the AMD polarity ambiguity as blocked. Only the report was written; native Git status remains unavailable due the Cygwin `FAST_CWD` failure.
- Completed the exhaustive read-only audit of `crates/vlapic/src/sources.rs` and wrote `tmp/review/vlapic-sources.md`. Accepted findings: two Critical (`SOURCES-C1` guest-controlled physical SMI can enter host SMM; `SOURCES-C2` vector-only ownership permits guest/device collisions and impossible Error recognition), eight High (software-disable, INIT reset, APIC mode/global-disable, trigger normalization, ExtINT semantics, two physical-EOI identity/order defects, and warning-only physical failure handling), and four Medium (unconditional seven-entry version, stale Delivery Status/Remote IRR, Performance LVT automatic mask divergence, and reprogram/arrival configuration races). The report includes the Intel-first basis, AMD SVM citation, qualified KVM comparison, complete semantic/state/event ledger, cross-file contracts, rejected candidates, test gaps, exact diagram accounting, and sole-output confirmation. No source files were modified; Git status remained unavailable because of the Cygwin `FAST_CWD` failure.
