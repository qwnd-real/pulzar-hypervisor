[package]
name = "apic-dump"
description = "Reads a pulzar guest's interrupt controllers out of the hypervisor underneath it"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
# The interface this tool exists to call: the ABI both sides agree on, and the
# instruction that carries it.
hypercall = { workspace = true }

[lints]
workspace = true
